# WhatsApp integration guide

This legacy filename is retained as a pointer. Use [META_GEREPLY_SETUP.md](META_GEREPLY_SETUP.md) for the official Meta Cloud API architecture.

The simulator remains available through the mock provider. It is not a real WhatsApp connection and must not be configured with Meta credentials.